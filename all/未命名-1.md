```mermaid
classDiagram
    %% ====================== 1. 根核心类：用户（对标原图顶层用户类） ======================
    class User{
        +userId: int
        +username: string
        +password: string
        +phone: string
        +registerTime: int
        +userStatus: enum
        +登录()
        +注册()
        +修改个人信息()
        +创建任务()
        +绑定用户画像()
    }

    %% ====================== 2. 核心业务主实体：待办任务（对标原图核心行程类） ======================
    class Task{
        +taskId: int
        +userId: int
        +taskTitle: string
        +taskContent: string
        +startTime: int
        +endTime: int
        +taskStatus: enum
        +taskCategoryId: int[]
        +新增任务()
        +编辑任务()
        +删除任务()
        +修改任务状态()
        +关联提醒()
        +获取任务详情()
    }

    %% ====================== 3. 附属实体：任务分类 ======================
    class TaskCategory{
        +categoryId: int
        +categoryName: string
        +categoryDesc: string
        +taskNum: int
        +获取分类下全部任务()
        +修改分类信息()
    }

    %% ====================== 4. 附属实体：任务提醒（对标原图电子登机牌） ======================
    class TaskRemind{
        +remindId: int
        +taskId: int
        +remindTime: int
        +remindType: enum
        +生成提醒()
        +触发提醒通知()
        +取消提醒()
    }

    %% ====================== 5. 用户专属实体：用户画像 ======================
    class UserProfile{
        +profileId: int
        +userId: int
        +taskTotalNum: int
        +completeNum: int
        +mainCategory: string
        +timePreference: string
        +behaviorData: int[]
        +统计用户行为()
        +更新画像数据()
        +获取画像总结()
    }

    %% ====================== 6. 后台服务类：AI分析服务（对标原图右上角支付领域服务） ======================
    class AIService{
        +serviceId: int
        +AI智能分类()
        +生成个性化推荐()
        +生成场景化建议()
        +分析用户行为()
    }

    %% ====================== 所有类之间关联关系 完全对标原图基数、连线、文字标注 ======================
    %% 1. 用户 1 --拥有--> 待办任务 1..n （原图核心用户-行程关系 1:1复刻）
    User "1" -- "1..n" : 拥有 > Task

    %% 2. 用户 1 --生成--> 用户画像 1 一对一强关联
    User "1" -- "1" : 生成 > UserProfile

    %% 3. 待办任务 1..n --归属--> 任务分类 1 （对标原图行程-酒店一对多）
    Task "1..n" -- "1" : 归属 > TaskCategory

    %% 4. 待办任务 1 --绑定--> 任务提醒 1..n （对标原图航班-座位一对多）
    Task "1" -- "1..n" : 绑定 > TaskRemind

    %% 5. AI服务 虚线依赖调用 所有业务实体 （完全对标原图支付服务虚线调用全部订单）
    AIService ..> Task : 调用
    AIService ..> UserProfile : 调用
    AIService ..> User : 调用
```